# ZTR_PROJ_ALV – Report ABAP com ALV Grid

Este projeto contém um report ABAP que exibe dados das tabelas `ZPROJ_ALV01` e `ZPROJ_ALV02` usando o `REUSE_ALV_GRID_DISPLAY`. O usuário pode escolher qual tabela visualizar por meio de radio buttons, com suporte a variantes, filtros (select-options) e clique duplo nas linhas.

---

## 📋 Funcionalidades

- ALV com padrão SAP (`REUSE_ALV_GRID_DISPLAY`)
- Alternância entre duas tabelas (`ZPROJ_ALV01` e `ZPROJ_ALV02`)
- Filtros via `SELECT-OPTIONS`
- Suporte a variante ALV (`P_VARIAN`)
- Mensagem informativa ao clicar duas vezes em uma linha
- Layout zebrado

---

## 🧾 Estrutura das Tabelas

### ZPROJ_ALV01

| Campo         | Tipo       | Descrição         |
|---------------|------------|-------------------|
| ID_REGISTRO   | CHAR/NÚM   | ID do registro    |
| DESC_REGISTRO | CHAR       | Descrição         |

---

### ZPROJ_ALV02

| Campo        | Tipo       | Descrição        |
|--------------|------------|------------------|
| CODIGO_ITEM  | CHAR/NÚM   | Código do item    |
| NOME_ITEM    | CHAR       | Nome do item      |

---

## 🧪 Como executar

1. Acesse a transação **SE38** no SAP GUI.
2. Crie um report chamado `ZTR_PROJ_ALV`.
3. Copie e cole o conteúdo do arquivo `ZTR_PROJ_ALV.abap`.
4. Execute (F8).
5. Selecione uma das tabelas (`P_TAB1` ou `P_TAB2`).
6. Preencha os filtros desejados.
7. Clique em **Executar**.

---

## 🖱️ Comportamento com Clique Duplo

- Em `ZPROJ_ALV01`: mostra mensagem com ID e descrição do registro
- Em `ZPROJ_ALV02`: mostra código e nome do item

---

## 📦 Requisitos

- SAP NetWeaver com suporte a `REUSE_ALV_GRID_DISPLAY`
- Tabelas `ZPROJ_ALV01` e `ZPROJ_ALV02` devidamente criadas

---

## 📄 Licença

Distribuído sob a licença MIT. Veja o arquivo `LICENSE` para mais detalhes.
